describe('/pet', () => {
  beforeEach('Create a new pet for each test');

  it('Verify pet creation by ID & status');

  it('Creating a pet with invalid keys or type values causes appropriate response errors');

  it('Can add an image to a pet');

  it('Can update a pet');

  it('Can delete a pet');
});
